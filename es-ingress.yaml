apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: kibana
spec:
  rules:
    - host: myapp.example.com
      http:
        paths:
          - path: /es
            backend:
              serviceName: elasticsearch
              servicePort: 9200